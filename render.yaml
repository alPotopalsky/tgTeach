services:
  - type: web
    name: tg-teach-bot
    runtime: python
    plan: free
    autoDeployTrigger: commit
    buildCommand: pip install -r requirements.txt
    startCommand: python mathbot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
